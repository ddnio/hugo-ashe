# Demo Site

这是 `hugo-ashe` 的展示站点内容目录。

- 本地预览：`hugo server --source exampleSite --themesDir ../.. --theme hugo-ashe`
- 构建输出：`exampleSite/public/`

GitHub Actions 会在 `main` 分支自动构建并发布该目录内容到 GitHub Pages。
